node_modules/
dist/

# Course PDFs at repo root (facts ship as TS; keep PDFs local-only)
/*.pdf

.DS_Store

.venv/
.venv_pdf/

# PDF text extraction artifacts (facts ship in src/react/data/examFacts.ts)
data/course/*_extracted.txt
data/course/generated_exam_facts.json

# ND / course release checkpoints (large .pt files)
weights/

# Course data downloads (large / reproducible via fetch script)
data/course/openweb10k.txt
# Large generated corpora (rebuild with python -m llm.expand_corpus)
data/corpus_expanded.txt

